#!/bin/bash -e
branch=$(git branch | sed -n -e 's/^\* \(.*\)/\1/p')
echo "Add all files and push to remote with branch name: $branch"
commit_message="$1"
git add . -A
git commit -m "$commit_message"
git push origin $branch

echo 'Done!'
